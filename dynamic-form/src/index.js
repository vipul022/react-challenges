import React from "react"
import ReactDOM from "react-dom"
import Form from "./components/Form"



// Dynamic form Challenge
// Build an app that accepts user input and displays it to the screen

// Allow the user to add an input field by clicking a button

// allow the user to remove an input field by clicking a button

ReactDOM.render(
  <Form />,
  document.getElementById("root")
  )